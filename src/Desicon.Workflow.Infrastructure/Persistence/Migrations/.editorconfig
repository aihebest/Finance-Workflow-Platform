# EF Core scaffolds this folder; only the *.Designer.cs files carry the
# <auto-generated /> marker that analyzers use to skip generated code.
# Mark the whole folder as generated so hand-authored code elsewhere in
# Infrastructure keeps full analysis instead of suppressing rules project-wide.
[*.cs]
generated_code = true
