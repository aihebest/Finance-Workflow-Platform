# .NET
bin/
obj/
*.user
TestResults/
coverage*.xml
*.trx

# Node
node_modules/
dist/
.vite/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
plan.txt
*.tfvars
!example.auto.tfvars.example
backend.hcl
!backend.hcl.example

# Secrets and local config
*.pfx
*.key
*.pem
appsettings.Development.json
appsettings.Local.json
.env
.env.*
!.env.example

# Security scan artefacts
#
# Root-anchored. Unanchored, "reports/" matches a directory of that name at
# ANY depth -- and Git on Windows compares case-insensitively, so it also
# matched tests/Desicon.Workflow.IntegrationTests/Reports/, silently excluding
# a whole test class from the commit on 22 Aug 2026.
#
# That failure is invisible from both ends: the build passes locally because
# the file is on disk, and CI passes because the file is absent and tests that
# do not exist cannot fail. The test count simply drops, and nobody counts.
/reports/
*.sarif

# IDE
.vs/
.idea/
*.swp
.DS_Store

# Azure Functions local development settings. Holds connection strings and
# AzureWebJobsStorage for the local host; never committed.
local.settings.json

# TypeScript incremental build cache. Machine-local; committing it makes every
# checkout differ and tells tsc things about a build that did not happen here.
*.tsbuildinfo

# Downloaded App Service logs (az webapp log download). Bulky, and they carry
# every SQL statement the app executed plus stack traces -- diagnostic output
# belongs in Application Insights, not in version control.
applogs/
*.zip

# The real org chart: staff names, addresses and reporting lines. Loaded by
# scripts/import-org-chart.ps1 and deliberately not versioned -- it is
# personal data that goes stale, and this repository is shared.
org-chart.csv
*-org-chart.csv
